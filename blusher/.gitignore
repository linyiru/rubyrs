# Generated by `rake compile` — the magnus ext (staged into lib/) and the
# carmine-ffi cdylib fallback (staged into ext/).
/lib/blusher.bundle
/lib/blusher.so
/lib/blusher.dylib
/ext/*.dylib
/ext/*.so
# rb-sys / rake-compiler build trees
/tmp/
/ext/blusher/target/
/*.gem
/Gemfile.lock
